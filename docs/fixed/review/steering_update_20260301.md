# Steering 文件更新报告

**日期**: 2026-03-01  
**更新人**: Kiro AI  
**更新文件**: `.kiro/steering/03-architecture-requirements.md`

---

## 📋 更新概览

本次更新补充了 3 个缺失的章节，完善了业务架构与需求对齐文档。

---

## ✅ 更新内容

### 1. 补充子账户明细表用途说明

**位置**: "3. 明细账簿层"章节

**补充内容**:
- 明确了 `t_sub_account_detail` 的用途：用于记录子账户（可用/冻结）的余额变动明细
- 说明了主要用于冻结/解冻场景的审计追溯
- 强调了冻结/解冻不生成凭证，直接操作子账户，通过子账户明细记录变动轨迹
- 包含 Pre/Post 余额快照，确保审计完整性
- 补充了三种明细表的区别：
  - `t_accounting_voucher_entry`（分录明细）：关注交易平衡，用于会计报表和科目总账
  - `t_account_detail`（账户明细）：关注账户变动轨迹，用于日终试算平衡和审计对账
  - `t_sub_account_detail`（子账户明细）：关注子账户变动轨迹，用于冻结/解冻审计

**更新前**:
```markdown
#### 3. 明细账簿层
- **结构**：`AccountDetail` (余额变动明细)
- **逻辑**：记录单个账户资金变动的轨迹，包含变动前后的余额快照（Pre/Post Balance），用于日终核对与审计对账
- **区别**：
  - `t_accounting_voucher_entry`（分录明细）：关注交易平衡，用于会计报表和科目总账
  - `t_account_detail`（账户明细）：关注账户变动轨迹，用于日终试算平衡和审计对账
  - 一条分录对应一条账户明细，但数据用途不同
```

**更新后**:
```markdown
#### 3. 明细账簿层
- **结构**：`AccountDetail` (账户明细)、`SubAccountDetail` (子账户明细)
- **逻辑**：记录单个账户资金变动的轨迹，包含变动前后的余额快照（Pre/Post Balance），用于日终核对与审计对账
- **子账户明细用途**：
  - `t_sub_account_detail` 用于记录子账户（可用/冻结）的余额变动明细
  - 主要用于冻结/解冻场景的审计追溯
  - 冻结/解冻不生成凭证，直接操作子账户，通过子账户明细记录变动轨迹
  - 包含 Pre/Post 余额快照，确保审计完整性
- **区别**：
  - `t_accounting_voucher_entry`（分录明细）：关注交易平衡，用于会计报表和科目总账
  - `t_account_detail`（账户明细）：关注账户变动轨迹，用于日终试算平衡和审计对账
  - `t_sub_account_detail`（子账户明细）：关注子账户变动轨迹，用于冻结/解冻审计
  - 一条分录对应一条账户明细，但数据用途不同
```

---

### 2. 补充记账自动开户流程

**位置**: "标准入账全流程"章节，在"阶段一：记账流水"之前新增"阶段零"

**补充内容**:
- 新增"阶段零：账户检查与自动开户（可选）"
- 详细说明了 8 个步骤：
  1. 账户存在性检查
  2. 遍历账户检查
  3. 自动开户触发
  4. 匹配开户模板
  5. 检查自动开户标识
  6. 执行自动开户
  7. 开户失败处理
  8. 继续记账流程
- 说明了应用场景：
  - 解决已经在线上运行的业务，没有事先开户的问题
  - 新客户首次交易时自动开户
- 引用了详细流程图：`docs/fixed/design/flowchat/auto_account_opening_flow.mmd`

**新增内容**:
```markdown
#### 阶段零：账户检查与自动开户（可选）
1. **账户存在性检查**：解析记账规则，获取涉及的账户列表
2. **遍历账户检查**：检查每个账户是否存在
3. **自动开户触发**：若账户不存在，触发自动开户流程
4. **匹配开户模板**：根据 `business_code` + `customer_type` + `subject_code` 匹配开户模板
5. **检查自动开户标识**：检查模板的 `auto_open` 字段是否为 1
6. **执行自动开户**：若支持自动开户，则创建 `t_account` 和 `t_sub_account`
7. **开户失败处理**：若模板不存在或不支持自动开户，记账失败
8. **继续记账流程**：所有账户准备就绪后，继续标准入账流程

**应用场景**：
- 解决已经在线上运行的业务，没有事先开户的问题
- 新客户首次交易时自动开户

**详细流程**：参考 `docs/fixed/design/flowchat/auto_account_opening_flow.mmd`
```

---

### 3. 补充期末结转流程

**位置**: "逻辑日切流程"章节，在"阶段 5：归档"之前新增"阶段 4.5"

**补充内容**:
- 新增"阶段 4.5：期末结转（可选）"
- 详细说明了前置条件、执行时机、执行内容
- 说明了 7 个执行步骤：
  1. 查询启用的结转规则
  2. 按执行顺序排序
  3. 遍历规则，查询源科目余额
  4. 计算结转金额
  5. 生成结转凭证
  6. 执行标准入账流程
  7. 记录结转记录
- 说明了 3 种结转类型：
  - 损益结转
  - 成本结转
  - 自定义结转
- 说明了失败处理：部分规则失败时触发告警，需要人工介入
- 引用了详细流程图：`docs/fixed/design/flowchat/period_end_transfer_flow.mmd`

**新增内容**:
```markdown
#### 阶段 4.5：期末结转（可选）
- **前置条件**：当前会计期间已关账，且未执行过期末结转
- **执行时机**：在"阶段 5：归档"之前执行
- **执行内容**：
  1. 查询启用的结转规则（`t_period_end_transfer_rule`，`status=1`）
  2. 按执行顺序排序（`ORDER BY execute_order ASC`）
  3. 遍历规则，查询源科目余额（支持通配符匹配，如 6* 匹配所有 6 开头的科目）
  4. 计算结转金额，根据 `transfer_direction` 确定借贷方向
  5. 生成结转凭证（借：源科目 贷：目标科目，或反向）
  6. 执行标准入账流程，更新源科目和目标科目余额
  7. 记录结转记录（`t_period_end_transfer_record`）
- **结转类型**：
  - 损益结转：将损益类科目（收入、费用）余额结转到本年利润科目
  - 成本结转：将成本类科目余额结转到相关科目
  - 自定义结转：根据业务需求自定义结转规则
- **失败处理**：部分规则失败时触发告警，需要人工介入

**详细流程**：参考 `docs/fixed/design/flowchat/period_end_transfer_flow.mmd`
```

---

## 📊 更新统计

| 更新项 | 更新前 | 更新后 | 说明 |
|-------|--------|--------|------|
| 子账户明细表用途 | 未说明 | 已说明 | 补充了用途、场景、区别 |
| 记账自动开户流程 | 未包含 | 已包含 | 新增"阶段零"，8 个步骤 |
| 期末结转流程 | 未包含 | 已包含 | 新增"阶段 4.5"，7 个步骤 |

---

## ✅ 验证检查

### 1. 内容完整性
- ✅ 子账户明细表用途说明完整
- ✅ 记账自动开户流程完整
- ✅ 期末结转流程完整

### 2. 逻辑一致性
- ✅ 与流程图一致（`auto_account_opening_flow.mmd`）
- ✅ 与流程图一致（`period_end_transfer_flow.mmd`）
- ✅ 与数据库表结构一致（`t_period_end_transfer_rule`, `t_period_end_transfer_record`）

### 3. 引用正确性
- ✅ 流程图路径正确
- ✅ 表名正确
- ✅ 字段名正确

---

## 🎯 后续工作

### ✅ 已完成
1. ✅ 更新 `.kiro/steering/03-architecture-requirements.md`（补充 3 个章节）

### ⏳ 待完成
2. ⏳ 手动应用修复到完整 SQL 文件（创建 `1-init-schema-fixed-v2.sql`）

---

## 📝 备注

- 本次更新完全基于 recheck 报告的建议
- 所有补充内容都与现有流程图和数据库表结构保持一致
- 更新后的 Steering 文件将自动包含在所有后续的代码生成任务中

---

**更新人**: Kiro AI  
**更新时间**: 2026-03-01  
**状态**: ✅ 已完成

