# Task 2: Balance Calculator & SpEL Sandbox
1. **BalanceCalculator**:
   - 实现泛化算法：根据 `AccountClass` 和 `direction` 判定加减。
   - 方法签名：`BigDecimal calculate(AccountClass clazz, DrCrFlag direction, BigDecimal amount, BigDecimal currentBalance, boolean isIncrease)`。
2. **SpEL Sandbox**:
   - 创建 `AccountingRuleEvaluator` 类。
   - 使用 `SimpleEvaluationContext` 配置安全沙箱。
   - **白名单**：只允许 `BigDecimal` 的加减乘除比较、`Math` 类常用函数、以及我们预设的 `DateUtils`。
   - 禁止任何反射、系统调用及未授权的方法执行。