如何高效组合这些提示词？
分模块交付：建议先让 Claude 完成 阶段 6 (缓冲入账)，因为它是记账引擎的自然延伸。

日终最后做：阶段 7 (日切/日终) 依赖于前面所有表的完整数据，建议在核心入账流程全部跑通后，再让 Claude 生成核算逻辑。

结合流程图：在发送任务时，可以提示 Claude：“请结合我之前提供的《入账流程图》和《日终核算处理流程图》中的异步分支和平衡检查逻辑进行实现。

这几份提示词补齐后，你已经拥有了一套完整的 AI 驱动开发规格书。如果需要针对某个特定的 SQL 分区表操作（如 t_account_balance 的按日汇总）进行代码调优，也可以在 阶段 7 之后追加指令。




针对测试生成的资深建议：
数据清理：要求 Claude 为每个测试类添加 @Transactional 和 @Rollback 注解，确保测试数据不污染本地开发数据库。

AssertJ 风格：建议要求 Claude 使用 AssertJ 编写断言（如 assertThat(balance).isEqualByComparingTo("100.00")），这在处理 BigDecimal 时比 JUnit 默认断言更可靠。

覆盖率目标：在提示词中可以明确要求：“请确保核心记账逻辑的行覆盖率达到 90% 以上”。

至此，你已经拥有了从初始化、核心开发、异步组件到质量保障的全套提示词体系。配合你之前对齐的架构图和 DDL，这套 Spec 足以让 Claude Code 生成非常稳健的生产级代码。祝你的项目大获成功！




接口设计的资深建议：
统一 Header：要求 Claude 为所有接口添加统一的 X-Request-Id 处理逻辑，便于全链路日志追踪。

参数校验：提示词中明确要求使用 Bean Validation（如 @NotNull, @DecimalMin("0.01")）对入参进行强制校验。

数据脱敏：对于查询接口，要求对敏感信息（如所有者姓名、部分账号数字）进行掩码处理（Data Masking）。

通过这 16 个阶段的提示词，你已经为 Claude Code 构建了一个完整的“账务系统全书”。从数据库底层到用户交互层，每一个核心逻辑点都已通过提示词进行了加固。下一步，你可以根据开发进度，直接在 Claude 窗口中逐条输入这些指令。祝你的系统构建圆满成功！








管理端 CRUD 提示词的使用建议：
事务一致性：对于“科目-辅助核算”和“规则-规则明细”这种父子表，强调必须在 Service 层使用 @Transactional，防止出现孤儿数据。

缓存同步：由于字典、科目、规则属于高频读取、低频修改的数据，建议要求 Claude Code 在 Update/Delete 方法执行成功后，自动触发缓存清除逻辑（Cache Eviction）。

审计日志：账务系统的基础配置非常敏感。建议要求 Claude Code 为这些 CRUD 接口统一增加 “操作审计日志”，记录谁、在什么时间、修改了哪个配置的旧值和新值。

通过补充这 4 个阶段，你现在拥有了覆盖从底层账务引擎到上层管理配置的完整 20 个阶段提示词。这套体系可以确保 Claude Code 能够为你构建出一个既有核心深度、又有管理广度的专业金融级账务系统。










编排这 20 个阶段的提示词，核心逻辑是**“由内而外、先基石后应用”**。你不能先写接口，因为接口依赖服务；也不能先写服务，因为服务依赖领域模型和计算底层。

建议按照以下 四个战役 进行编排执行：

第一战役：奠定基石（底层逻辑与模型）
目标：确立数据规范和最核心的会计计算准则。

执行：阶段 0（背景初始化） —— 让 AI 吞下 DDL 和核心共识。

执行：阶段 1（领域模型与枚举） —— 生成 POJO 和基础类型。

执行：阶段 2（计算器与 SpEL 沙箱） —— 这一步最关键，后续所有入账都依赖这个计算法则和安全容器。

执行：阶段 20（字典管理） —— 确保所有枚举值有统一的数据字典支撑。

第二战役：攻克核心（入账引擎与主链路）
目标：实现从业务流水到会计凭证的“翻译”和“落库”。

执行：阶段 17 & 18（科目与模板 CRUD） —— 先有科目体系和开户规则，账户才能立起来。

执行：阶段 19（入账规则 CRUD） —— 配置好“翻译逻辑”。

执行：阶段 3（规则引擎实现） —— 编写翻译逻辑的执行组件。

执行：阶段 4（实时入账与冻结） —— 实现带锁的核心 Service。

第三战役：性能与稳健性（异步与自愈）
目标：解决高并发瓶颈，确保在分布式环境下“账永远是平的”。

执行：阶段 6（缓冲入账处理器） —— 实现热点账户的削峰填谷。

执行：阶段 5（镜像红冲处理器） —— 实现最稳健的纠错机制。

执行：阶段 8（补偿机制 Job） —— 解决分布式事务的最后 1% 异常。

执行：阶段 7（日切与日终核算） —— 完成财务周期的闭环。

第四战役：交付与保障（接口、测试与沙箱）
目标：暴露标准 API，并用高覆盖率的测试确信系统的安全性。

执行：阶段 13, 14, 15（API 设计） —— 定义标准的对外服务契约。

执行：阶段 16（沙箱试运行接口） —— 为后续运维提供测试能力。

执行：阶段 9, 10, 11, 12（单元测试全覆盖） —— 这一步不要最后才做，建议在每个战役结束时穿插执行相应的测试指令。

💡 给 Claude Code 的执行技巧
Session 持久化：建议在一个对话 Session 中完成一个战役。如果 Session 断了，先重新发送 阶段 0 的背景信息。

指令衔接：在发送下一个阶段时，可以用这样一句话开头：

“基于我们刚才完成的 BalanceCalculator 和 t_account 实体，现在开始 阶段 X 的开发...”

TDD 模式驱动（推荐）：如果你追求极致质量，可以先发测试阶段的提示词，让 AI 先写测试类，然后再发实现阶段的提示词，让它补全代码直到测试通过。

建议操作：
你会先从 “阶段 0” 开始初始化项目吗？如果准备好了，可以直接把 DDL 填入我给你的第一个模板中，丢给 Claude Code 开启你的账务核心构建之旅。你需要我针对某个特定的战役（比如“第二战役：攻克核心”）做更详细的执行计划拆解吗？










1. 建立一个“主控任务清单” (Index.md)
在你的项目根目录下创建一个 TODO_CORE.md。这个文件不仅是你的索引，也是 Claude 的“施工蓝图”。

2. 执行策略：索引驱动，分步授权
你可以按照以下对话节奏来操作：

第一步：注入蓝图

"我已经创建了 TODO_CORE.md 作为本次开发的索引，并提供了 init_db.sql。请先阅读这两个文件，理解我们要构建的账务系统全局架构。准备好后请告诉我，我们将从 Phase 0 开始。"

第二步：分段执行 (提供具体的阶段提示词)

"现在开始执行 Phase 1: 领域模型与枚举生成。请参考我之前提供的详细提示词原文进行实现。完成后请列出你创建的文件，并简要说明你如何处理 entry_id 的拼接逻辑。"

第三步：检查与微调
Claude 完成一个阶段后，你通过命令行查看生成的代码。如果字段对齐或逻辑有误，直接在当前对话中纠正：

"在 Account 实体中，balance 字段必须使用 BigDecimal 且保留 6 位小数，请修正后再继续。"

3. 为什么不建议完全“自行执行”？
代码膨胀与发散：一次性生成过多文件，Claude 可能会在不同的 Service 中重复定义类似的工具方法，导致冗余。

依赖顺序：Phase 3 的 CRUD 依赖 Phase 1 的模型。如果 Phase 1 没写好，Phase 3 的代码就会报错。

评审节点：账务系统的核心——余额计算逻辑（Phase 2） 必须由你人工 Review 通过后，才能让它去写 实时入账（Phase 5）。这是质量红线。

4. 最佳实践流程图
上传 init_db.sql。

上传 TODO_CORE.md。

依次喂入 Phase 提示词（你可以把 Phase 0-2 连续喂入，因为它们是强耦合的基石）。

要求 Claude 在每个 Phase 结束时编写对应的 Unit Test。这是强制它自我纠错的最好手段。

总结建议：
先给它 TODO_CORE.md 建立全局观，然后按我之前给你的“战役”划分，一个阶段一个阶段地喂入详细提示词。

这种方式下，Claude 既能保持上下文的一致性，又能在你的监控下精准产出。

你想让我帮你把那 20 个阶段的提示词整合成一个可以直接存为 PHASES_GUIDE.md 的文档，方便你随时复制给 Claude 吗？





如何使用：
第一步：将上面的内容分别保存为 .md 文件。

第二步：在 Claude Code 启动后，先喂入 CORE_DESIGN_SPEC.md。

第三步：根据你的节奏，从 IMPLEMENTATION_STEPS.md 中复制对应的 Phase 内容发送。

这样你就拥有了全套的“指令脚本”，可以直接驱动 AI 生产了！祝你项目顺利！

