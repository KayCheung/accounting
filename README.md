# accounting
这是一个高可靠、可扩展的金融级账务核心系统，基于会计准则设计，支持多级科目树（资产/负债/权益等）、辅助核算（客户/项目/合同等）、内外部账户统一管理，并内置可用/冻结双子账户机制。系统提供实时与异步（缓冲）记账模式，通过规则引擎（SpEL脚本）驱动凭证自动生成，完整覆盖业务流水 → 凭证 → 分录 → 明细账 → 余额更新全链路。支持单边记账、红蓝字调账、幂等控制与事务一致性，日终自动汇总生成日余额及快照，满足对账、监管与报表需求。适用于银行、支付、小贷等场景。