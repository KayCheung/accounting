<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kltb.accounting.core.infrastructure.persistence.mapper.SubAccountMapper">

    <!-- P1-2: 显式 ORDER BY 保证加锁顺序，防止死锁 -->
    <select id="selectForUpdate" resultType="com.kltb.accounting.core.infrastructure.persistence.entity.SubAccountPO">
        SELECT id, account_no, balance_type, balance_direction, balance, version,
               create_time, update_time, is_delete, tenant_id
        FROM t_sub_account
        WHERE account_no = #{accountNo}
          AND is_delete = 0
        ORDER BY account_no ASC
        FOR UPDATE
    </select>

</mapper>
