<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kltb.accounting.core.infrastructure.persistence.mapper.AccountingVoucherAuxiliaryMapper">

    <!-- 批量插入辅助核算项（Step 18 P0-6） -->
    <insert id="batchInsert">
        INSERT INTO t_accounting_voucher_auxiliary
            (voucher_no, entry_id, subject_code, aux_type, aux_code, aux_name,
             change_direction, amount, accounting_date, tenant_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.voucherNo}, #{item.entryId}, #{item.subjectCode}, #{item.auxType},
             #{item.auxCode}, #{item.auxName}, #{item.changeDirection}, #{item.amount},
             #{item.accountingDate}, #{item.tenantId})
        </foreach>
    </insert>
</mapper>
